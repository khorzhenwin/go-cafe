# AWS RDS (shared cluster – same host/db as gold-digger; go-cafe tables use prefix gocafe_)
DB_HOST=gochujang-db.chkq4eeqqrb5.ap-southeast-1.rds.amazonaws.com
DB_PORT=5432
DB_NAME=gochujang
DB_USER=postgres
DB_PASSWORD=<Enter_DB_Password>
DB_SSL=disable
# Path to RDS CA bundle (cert is in backend root: global-bundle.pem)
DB_SSL_ROOT_CERT=global-bundle.pem

# JWT (required for auth)
JWT_SECRET=<your-secret-at-least-32-chars>
# Optional; default 24h
# JWT_EXPIRY=24h
